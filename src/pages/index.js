import { MenuContainer } from "@/components/MenuContainer";

const Home = () => {
  return (
    <div className="mainContainer">
      <MenuContainer />
      <MenuContainer />
      <MenuContainer />
    </div>
  );
};

export default Home;
